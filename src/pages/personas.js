import React from 'react';

const Personas = () => {
  return (
    <div
      style={{
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        height: '90vh'
      }}
    >
      <h1>Personas</h1>
    </div>
  );
};

export default Personas;
